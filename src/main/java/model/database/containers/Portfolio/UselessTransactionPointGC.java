package model.database.containers.Portfolio;

public class UselessTransactionPointGC {
    /**
     * Removes {@link model.portfolio.transaction.TransactionPoint} when they are no longer required, either by law nor
     * by information (any time the data cancels out)
     */
}
